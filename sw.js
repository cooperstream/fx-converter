// Базовий Service Worker для подальших PWA-кроків.
// На цьому етапі — без кеш-логіки, щоб не змінювати поточну поведінку застосунку.
self.addEventListener('install', () => {
  // no-op
});

self.addEventListener('activate', () => {
  // no-op
});
